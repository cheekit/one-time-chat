import React from 'react';
import { Toolbar } from '../../../components';
import {
  HomeControl
} from '../../controls';

function ChatToolbar() {
  return (
    <Toolbar>
      <HomeControl />
    </Toolbar>
  );
}

export default ChatToolbar;
